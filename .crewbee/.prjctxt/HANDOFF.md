# Session Handoff

## Current Snapshot

- Active cycle C1 is complete; latest request committed and pushed the approved website refresh/tooling to `main` as `fd28a7e Add section render tooling and assets`.
- Generic homepage section-render tooling/scripts/docs are in place, replacing prior ad-hoc render naming and keeping generated PNGs ignored by default.
- Top navigation anchor jumps still rely on `.anchor-target` `scroll-margin-top` as the single sticky-header offset, with global `scroll-padding-top` zeroed; the desktop offset is 160px and mobile is 136px so labels/titles/headings or core content starts land lower below the sticky header without stacked extra top space.
- Homepage refresh and equivalent visual/component refactor are committed and pushed.
- The site now presents CrewBee with v2.2.2 bilingual messaging: harness-adaptable Agent Team assets for agent harnesses, with OpenCode framed as the first/current adapter rather than the product boundary, and with Coding Team and Project Context sections retained.
- Repeated card/accent/design classes have been consolidated into shared design-system config and small presentation components.
- A functionally equivalent naming cleanup is in place: the `homepageContent` module is the content loader/type boundary, section props use `content`, and product section names align with their current responsibilities.
- A small equivalent component-boundary cleanup is in place: unused design-system entries/type export are removed, `FeatureCard` no longer exposes React `key`, and list keys are held by caller-side fragments.
- Same-style visual polish is in place to reduce long-page/card density pressure: compact content rhythm, tighter section padding, calmer buttons/cards, natural Hero text sizing, refined heading/body line heights, constrained long highlight copy, denser Coding Team/Project Context/template cards, and smoother responsive gaps.
- English large-scale italic styling has been removed from primary headings/subtitles/cards/FAQ and section copy; only a small number of emphasis cases remain italic, and Chinese remains covered by the normal-style override.
- Hero right-side visual is aligned with the published site: the canonical CrewBee logo image is centered with square responsive sizing and the floating labels now describe Define Team / Pick Leader / Delegate / Review and localized equivalents.
- Hero and Navbar width now match the published-site rhythm more closely with 1400px max-width containers, medium-breakpoint horizontal padding, and restored Hero grid spacing.
- Final CTA now includes GitHub, Quick Start, Workflow, and Discussions entries; primary/get-started CTAs target Installation, Workflow lands on a not-implemented placeholder until a real destination exists, and the OpenCode integration CTA row uses real links/anchors.
- v2.2.2 UI/content adjustments are in place: Core Features uses a centered 2x2 grid, Team Assets uses a file tree with descriptions, First Task provides Quick Start prompt-display onboarding, Builder Audience supports five cards, FAQ/Roadmap/Trust/How wording is harness-neutral, Project Context flow title is localized, and in-page anchors account for the sticky header.
- Latest story-order update is in place: a new What Works Today section highlights current usable OpenCode workflows, the rationale/problem section includes Before/After comparison, First Task now reads as a three-step Quick Start plus prompt card, Product Highlights/Team Templates sit after Coding Team, Roadmap is later and visually/verbally weaker as a future-looking section, and OpenCode-ready copy remains bounded by harness-adaptable positioning.
- Latest copy pass removes MIT/open-source license language from Hero, Trust Strip, and Final CTA selling points; those surfaces now emphasize Agent Team assets, harness adaptability, and OpenCode as the first adapter. Low-priority license/footer information can remain.
- Recent layout/content pass restored the Open Source Inspiration section, updates How It Works to distinguish Leader and Executor paths, compacts the Project Context/Next roadmap into a balanced timeline/card layout, changes the FAQ title to `FAQ`, points Navbar Quick Start at the First Task anchor, and makes Hero use small-viewport-safe height while keeping later sections natural.
- Latest layout pass moves First Task directly after Coding Team as a compact 40/60 quick-start with centered prompt card, and moves Open Source Inspiration after FAQ as a light full-width acknowledgement before the closing CTA.
- Latest bilingual content pass keeps `homepageContent.zh` structurally and semantically aligned with `homepageContent.en`, including updated site/features/how/Coding Team/First Task/CTA copy and localized Leader/Executor path labels.
- Latest warm visual polish is in place: dark theme tokens are warmer, card borders/hover glow are honey-toned, Navbar labels are slightly larger/tighter and hover honey, the Hero visual has subtle rings, Hero floating labels now describe `Define Team / Pick Leader / Delegate / Review`, and Demo CTAs read `View Workflow` / `查看工作流` until a real demo video exists.
- Latest Installation / Quick Start onboarding is in place near the top of the homepage: it provides a copyable `npx crewbee@latest setup --with-opencode` command, bilingual setup/open-project/Coding-Team steps, and user-level/no-project-node_modules/no-manual-config/setup-verification messaging; the main nav/get-started CTA now targets `#installation` and displays `Install CLI` / `安装 CLI`.
- Latest anchor-positioning fixes are in place: ids for Installation, Why CrewBee, Coding Team, How It Works, Team Templates, Project Context, and Docs/FAQ were moved to visible heading/content targets rather than large padded outer sections, the final correction keeps only `.anchor-target` `scroll-margin-top` as the effective offset while zeroing global `scroll-padding-top`, and the latest follow-up raises that single offset to 160px desktop / 136px mobile; `#first-task` also uses the shared anchor-target behavior.
- Latest launch-readiness cleanup is in place: Footer License was removed; installation guide links point to the GitHub installation guide; public doctor-check and adapter-doc CTAs were removed or reworded as setup verification; Docs links route to a bilingual not-implemented Docs placeholder; Workflow CTAs route to a bilingual not-implemented Workflow placeholder; navbar logo links to `#top`; First Task no longer exposes page-side Copy Prompt controls.
- Latest theme initialization fix is in place: the root `dark` class is applied before React mount unless `localStorage.theme` is explicitly `light`, so default first paint is dark without overriding a saved light choice.
- Latest Discussion-link placement update is in place: Installation no longer has a discussion link, and the final CTA contains both Welcome Discussion and first-task sharing links with bilingual copy.
- Latest Quick Start visual hierarchy refinement is in place: the standalone note box became three lightweight trust points, the command card is flatter and more focused with a concise command note, setup steps are a lighter horizontal step flow, and the bottom follow-up copy uses one stable sentence plus two lightweight links.
- Latest Quick Start follow-up is in place: desktop Installation now reads more like a full conversion section with increased min-height/padding/gaps, and the command highlight uses a calmer honey (`#C98512`) with medium weight and no glow/text-shadow.
- Latest section-render tooling is in place: npm scripts can render local or published localized homepage sections via Playwright, the tool supports CLI/env overrides and `--help`, docs describe usage, and generated section PNGs default to ignored local artifacts.
- Latest homepage content assetization is in place but not yet committed: bilingual homepage wording now lives in per-locale external content assets (`homepage-content.en.json` and `homepage-content.zh.json`) with maintenance notes, the TypeScript content module imports both locale files and preserves the same `{ en, zh }` export shape, TypeScript JSON module resolution is enabled, and remaining visible brand/label strings in shared layout/section/logo surfaces read from centralized content.
- Project Context is intentionally labeled as Next/next-stage capability.
- Latest uncommitted v2.2.2 content-asset wording pass updates only the per-locale homepage content assets, strengthening Hero, why/current capability, workflow, Coding Team, product highlights, team templates, Quick Start, roadmap, and final CTA copy; it keeps OpenCode as the first/current adapter, CrewBee as the Agent Team asset layer, Project Context as roadmap/next-stage, and the “simple stays fast; complex gets support” line in both languages. No layout/visual structure was intentionally changed.
- Parent-session verification recorded: `npm run lint` passed, `npm run build` passed, follow-up lint/build after typing, naming, component-boundary cleanup, visual polish, v2.2.1 copy/CTA update, English italic-reduction pass, Hero logo alignment, Hero/Navbar width alignment, latest v2.2.2 harness-adaptable pass, story-order/What Works Today pass, MIT-license demotion copy pass, Inspiration/Leader-Executor/Roadmap/FAQ/Quick Start pass, latest compact First Task/Inspiration placement pass, latest bilingual parity pass, latest warm visual/CTA wording polish, latest Installation / Quick Start update, anchor-positioning fixes, latest launch-readiness cleanup, latest default-dark pre-mount theme initialization fix, latest concrete Discussion-link placement update, latest Installation / Quick Start visual hierarchy refinement, latest Quick Start desktop rhythm/code-highlight follow-up, latest section-render tooling update, latest homepage copy assetization, latest homepage content asset rename, latest per-locale homepage content split, and latest v2.2.2 content-asset wording pass passed. Latest content-asset wording pass reported `npm test --if-present`, `npm run lint`, and `npm run build` passed, with independent review reporting no blocker. Section rendering against the published site also succeeded and produced 15 PNGs with published dark-theme rendering confirmed. Latest pushed baseline reported clean status and `main...origin/main` synchronized before the uncommitted assetization/rename/split/content-pass work. No real-device/browser preview screenshot pass was performed for the latest content-only pass.
- Remaining launch caveat: Docs and Workflow are placeholders until real destinations exist.

## Exact Next Actions

1. Review and commit the latest homepage content assetization/rename/split plus v2.2.2 content-asset wording pass when ready; it was reported complete and verified but uncommitted.
2. For wording edits, update `content/homepage-content.en.json` and/or `content/homepage-content.zh.json` first and preserve bilingual structural/semantic parity.
3. Replace Docs/Workflow placeholders with real destinations when available, preserving bilingual copy parity.
